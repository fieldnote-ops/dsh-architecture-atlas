# 参与维护

欢迎补充证据、纠错或跟进新的 DSH 版本。这个项目优先接受“可定位、可复查”的修改。

## 证据要求

- 架构事实应指向 DSH、Cordis 或论文的公开文件与明确版本；
- 把“论文保证”“实现机制”“作者推断”分开描述；
- 不用星数、热度或二手转述证明技术性质；
- Developer Preview 中尚未验证的能力必须显式标注边界。

## 版本规则

- 旧版本是研究快照，不用新事实静默覆盖；
- 上游语义或证据发生实质变化时，新增 `data/versions.js` 版本对象；
- 纯样式、无障碍和拼写修复可直接更新站点代码，并记录在 `CHANGELOG.md`；
- 每个新版本都要更新 DSH 版本、commit、Cordis 版本、论文草稿日期和核对日期。

## 受众 / 阅读者心智核查

公开页面的每一段文字和每一个交互，都必须先回答：它帮助读者理解什么、判断什么或继续阅读什么？

- 公开页面写给第一次接触 DSH 的读者，不写给内容采集者、产物生成者或发布维护者；
- 检索词、过滤规则、归档操作、构建状态和审核流程默认留在维护侧，除非读者需要它来判断内容可信度；
- 动效必须解释顺序、因果或状态变化。静态布局已经足够时，不增加播放、暂停、进度条或自动滚动；
- 发布前从首页到目标页面连续阅读，检查标题、按钮和说明是否会让人误认本站为 DeepSeek Harness 官方；
- 任何不能说明读者收益的元素都应删除或折叠到次要位置。

提交前运行：

```bash
npm test
```
